/** Matches client-web cookie key `cart_token` for anonymous cart continuity. */
export const GIVE_GIFT_CART_TOKEN_KEY = 'cart_token';

/** Matches client-web `localStorage` key used after creating a present. */
export const PRESENT_STORAGE_JSON_KEY = 'present';
