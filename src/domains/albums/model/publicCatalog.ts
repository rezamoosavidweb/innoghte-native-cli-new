/**
 * Public course lists are filtered by `category_id` on
 * `GET api/v1/public/courses` (same as client-web).
 * Public drawer album slice uses **`category_id=9`**.
 */
export const PUBLIC_ALBUM_CATEGORY_ID = 9;
