/**
 * Stable MMKV / persist keys used across bootstrap and domain stores.
 * Must stay in sync with `domains/settings` persist `name` fields.
 */
export const LANGUAGE_STORAGE_KEY = 'awesome-language';

/** Set to `true` after the user dismisses the startup/onboarding screen. */
export const STARTUP_SEEN_KEY = 'innoghte-startup-seen';
