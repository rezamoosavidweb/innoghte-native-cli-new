/**
 * Re-export port types from contracts (single public surface for the purchase plugin).
 */
export type {
  IsProductPurchased,
  RegisterProductPurchaseLookup,
} from '@/shared/contracts/purchases';
