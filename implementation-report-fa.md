# گزارش پیاده‌سازی و تست اپلیکیشن

## نتیجه

نسخهٔ اندروید با موفقیت build و روی `emulator-5554` نصب و اجرا شد. ورود با حساب استیج انجام شد، تم دارک فعال بود و سناریوهای اصلی سبد خرید، صفحات قابل خرید، هدیه و تیکت روی دیوایس بررسی شدند.

## تطبیق صفحات وب و اپ

صفحه‌ها و جریان‌هایی که قبلاً placeholder بودند یا در اپ معادل کامل نداشتند، به پیاده‌سازی واقعی متصل شدند:

| وب | اپ React Native |
|---|---|
| کتاب/کتاب صوتی، از جمله «هنر زندگی کردن» و جزئیات آن | `AudioBooksScreen` و `PublicAudioBookDetailScreen` |
| Podcast | `PodcastScreen` با داده و پخش صوت |
| Meditation | `MeditationScreen` |
| Reading | `ReadingScreen` |
| Listening | `ListeningScreen` |
| Writing | `WritingScreen` و فرم ارسال نوشته |
| Tutorial | `TutorialScreen` |
| Private consultation | `PrivateConsultationScreen` |
| Support service | `SupportServicesScreen` |
| Gift given / gift received | `GiftSubScreen` با دادهٔ API |
| Public live/events and product details | صفحات لیست/جزئیات واقعی و CTA سبد خرید |
| About / Hossein intro | صفحهٔ About موجود اپ |
| Payment result variants | `PaymentResultScreen` با deep link درگاه |

مسیرهای ورود، ثبت‌نام، بازیابی رمز، پروفایل، امنیت، سبد، پرداخت، تراکنش‌ها، FAQ، تماس، همکاری، قوانین، حق نشر، دوره‌ها، آلبوم‌ها، جلسات زنده و رویدادها نیز در اپ موجود و به route واقعی متصل هستند.

## سبد خرید

- CTA مشترک «افزودن به سبد خرید» در کارت‌های دوره، آلبوم، کتاب صوتی، جلسهٔ زنده و رویداد استفاده می‌شود.
- CTA سبز در سمت راست و «اطلاعات بیشتر» در سمت چپ قرار دارد.
- هدر صفحات قابل خرید از کامپوننت مشترک basket استفاده می‌کند و تعداد واقعی خطوط سبد را نشان می‌دهد.
- روی دیوایس سناریوی `افزودن → badge برابر ۱ → مشاهده در سبد → حذف → سبد خالی` موفق بود.
- endpoint حذف تک‌آیتم استیج در بک‌اند با 500 و پیام null بودن `$cartToken` خراب است. این خطا با درخواست مستقل هم بازتولید شد. برای موبایل fallback محدود اضافه شد که با endpoint سالم حذف کل سبد، فقط خطوط باقی‌مانده را بازسازی می‌کند.

## هدیه به دیگری

- جریان اپ با رفتار وب تطبیق داده شد: ساخت present، ذخیرهٔ mapping، پاک‌کردن سبد قبلی، افزودن محصولات هدیه و رفتن به سبد خرید.
- اگر پس از یک سبد هدیه، محصول عادی افزوده شود، سبد هدیه پاک و به سبد normal تبدیل می‌شود؛ زیرا checkout فقط یک `order_type` می‌پذیرد.
- فرم هدیه، راهنما و فیلدهای گیرنده روی دیوایس نمایش داده شدند. ساخت هدیه و ارسال به درگاه عمداً اجرا نشد تا داده/تراکنش واقعی استیج ایجاد نشود.

## تیکت

- لیست تیکت و empty state بررسی شد. مشکل پنهان شدن «تیکت جدید» برای حساب بدون تیکت اصلاح شد.
- ایجاد تیکت شامل عنوان، توضیحات، دسته‌بندی و اولویت مطابق وب است.
- آپلود چندفایلی تا ۵ فایل، حداکثر ۲ مگابایت برای هر فایل و فرمت‌های JPG/JPEG/PNG/PDF اضافه شد.
- انتخاب دسته‌بندی و اولویت، باز شدن document picker، افزودن فایل و حذف فایل روی دیوایس موفق بود.
- reply و attachment پاسخ در کد و FormData پیاده شده‌اند؛ حساب استیج تیکت موجود نداشت، بنابراین پاسخ واقعی در UI قابل اجرا نبود.
- برای جلوگیری از ایجاد دادهٔ خارجی، تیکت آزمایشی واقعاً submit نشد.

## تم و QA بصری

- تم پیش‌فرض برای نصب/کاربر جدید دارک است؛ انتخاب صریح ذخیره‌شدهٔ کاربر همچنان محترم می‌ماند.
- مقایسه‌های هم‌اندازه در `qa-artifacts/courses-reference-vs-device.png` و `qa-artifacts/live-reference-vs-device.png` ذخیره شدند.
- گزارش کامل QA بصری در `design-qa.md` قرار دارد.

## اعتبارسنجی

- Android debug build: موفق (`BUILD SUCCESSFUL`، 537 task)
- نصب APK روی `emulator-5554`: موفق
- TypeScript: `npx tsc --noEmit` موفق
- ESLint فایل‌های تغییرکرده: موفق
- Metro bundle و اجرای اپ: موفق
- Jest موجود پروژه دو بار، حتی با `--forceExit`، بدون تولید نتیجه تا timeout ادامه پیدا کرد؛ بنابراین به‌عنوان تست پاس‌شده گزارش نمی‌شود.

## محدودیت‌های تست استیج

- checkout/درگاه پرداخت و ساخت هدیه اجرا نشدند چون اثر خارجی و مالی دارند.
- تیکت واقعی ثبت نشد و به علت خالی بودن حساب، reply واقعی قابل تست نبود.
- مشکل حذف تک‌آیتم، ایراد بک‌اند استیج است؛ workaround موبایل روی دیوایس موفق تست شد، اما endpoint سرور همچنان باید اصلاح شود.
