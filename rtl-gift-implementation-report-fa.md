# گزارش پیاده‌سازی RTL، هدر سبد و فرم هدیه

## نتیجه

- کنترل سمت چپ هدر در تمام مسیرهای اپ به کامپوننت مشترک سبد خرید تبدیل شد و تعداد واقعی آیتم‌های سبد را نمایش می‌دهد.
- رفتار RTL در کامپوننت‌های متنی و ورودی‌های مشترک اصلاح شد؛ ورودی‌های ذاتاً LTR مانند ایمیل، موبایل و اطلاعات کارت بانکی استثنای صریح دارند.
- انتخاب دوره و آلبوم در فرم هدیه از چک‌باکس‌های جداگانه به مولتی‌سلکت مشترک تبدیل شد.
- با بررسی فرم استیج وب، دو گروه «روی‌خط‌ها» و «کتاب‌ها» نیز به مولتی‌سلکت‌های فرم هدیه اپ اضافه شدند.
- پسوند «عمومی» از عنوان‌های قابل مشاهده دوره‌ها و آلبوم‌ها حذف شد.

## بررسی روی دیوایس

- صفحه هدیه، هدیه‌های ارسالی، پروفایل، سبد خرید، فهرست تیکت و ثبت تیکت روی `emulator-5554` بررسی شدند.
- وجود آیکون سبد در بالا-چپ و badge تعداد در مسیرهای مختلف تأیید شد.
- انتخاب هم‌زمان دو دوره در مولتی‌سلکت و نمایش تعداد انتخاب‌ها تأیید شد.
- فهرست تیکت و فرم ثبت تیکت از نظر راست‌چین بودن عنوان، placeholder و ساختار بررسی شدند.
- ساخت تیکت، ثبت هدیه و پرداخت واقعی اجرا نشد تا داده یا تراکنش ناخواسته در استیج ایجاد نشود.

## اعتبارسنجی فنی

- `tsc --noEmit`: موفق
- ESLint فایل‌های تغییرکرده: موفق
- Metro و اجرای اپ روی امولاتور: موفق
- خطای مرتبط در logcat پس از بارگذاری مجدد مشاهده نشد.

## شواهد

- `qa-artifacts/gift-reference-vs-implementation-final.png`
- `qa-artifacts/gift-course-multiselect-two-selected.png`
- `qa-artifacts/ticket-create-rtl-fixed.png`
- جزئیات کامل QA در `design-qa.md` ثبت شده است.
