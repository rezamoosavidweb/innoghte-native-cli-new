/**
 * Fonts under `src/assets/font` are bundled into native projects when you run
 * `npx react-native-asset` (see devDependency `react-native-asset` and npm script).
 */
module.exports = {
  assets: ['./src/assets/font/'],
};
